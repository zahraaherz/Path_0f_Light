# Add these dependencies to your pubspec.yaml file

dependencies:
  # For image caching
  cached_network_image: ^3.3.0

  # For image zoom/pan viewing
  photo_view: ^0.14.0

  # For file picking (PDF and images)
  file_picker: ^6.1.1

  # Already have these, but make sure they're included:
  firebase_storage: ^11.5.6
  cloud_firestore: ^4.13.6
  cloud_functions: ^4.5.12
